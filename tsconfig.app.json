//tsConfig ana yapılandırmayı yaparken, tsconfig.app ise uygulama yapılandırmasını sağlar. BU da tsconfig'den çeker ayarları ve ng serve için varlığı zorunludur.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": []
  },
  "include": [
    "src/**/*.ts"
  ],
  "exclude": [
    "src/**/*.spec.ts"
  ]
}
